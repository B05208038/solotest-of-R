---
title: "midterm hw 3"
author: "OwenYeh"
date: "2017¦~1¤ë19¤é"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
##¸ê®Æ·§Æ[³ü
§Ú¨Ï¥Î 'stackloss'¸ê®Æ¶°¡A¸ê°T¦p¤U:
```{r}
str(stackloss)
summary(stackloss)
```
##´²¥¬¹Ï
§Q¥Îggplot2 Ã¸µeªº´²¥¬¹Ï
```{r echo = FALSE, message=FALSE}
library(plotly)
library(ggplot2)
static_gg00<-ggplot(stackloss, aes(x = Air.Flow, y = Water.Temp, color = stack.loss)) +
  geom_point()+
  ggtitle("stack loss vs the position of the environment") +
  xlab("Air Flow") +
  ylab("Water Temperature")
ggplotly(static_gg00)

```

##¸ê®Æ·§Æ[¶L
§Ú¨Ï¥Î 'pressure'¸ê®Æ¶°¡A¸ê°T¦p¤U:
```{r}
str(pressure)
summary(pressure)
```
##ÂI½u¹Ï
§Q¥Îggplot2 Ã¸µeªºÂI½u¹Ï
```{r echo = FALSE, message=FALSE}
library(ggplot2)
library(plotly)
static_gg01<-ggplot(pressure, aes(x = temperature, y = pressure)) + 
  geom_point() +
  geom_line()

ggplotly(static_gg01)
```





##¸ê®Æ·§Æ[°Ñ

```{r echo = FALSE, message=FALSE}
library(ggplot2)
library(plotly)
```

§Ú¨Ï¥Î 'mpg'¸ê®Æ¶°¡A¸ê°T¦p¤U:
```{r}
str(mpg)
summary(mpg)
```
##ªø±ø¹Ï
³o¬O¤@­Ó¥Î'ggplotly()' ¥[¤J¤¬°Ê©Êªºªø±ø¹Ï

```{r}
library(plotly)
static_gg <- ggplot(mpg, aes(x = class)) +
  geom_bar(aes(fill = drv))
ggplotly(static_gg)
```

##¸ê®Æ·§Æ[¸v
§Ú¨Ï¥Î 'InsectSprays'¸ê®Æ¶°¡A¸ê°T¦p¤U:
```{r}
str(InsectSprays)
summary(InsectSprays)
```
##²°Å½¹Ï
§Q¥Îggplot2 Ã¸µeªº²°Å½¹Ï
```{r echo = FALSE, message=FALSE}
library(plotly)
library(ggplot2)
static_gg1<-ggplot(InsectSprays, aes(x = spray, y = count)) +
  geom_boxplot() +
    ggtitle("the quantity of bugs vesus different kind of sprays")
ggplotly(static_gg1)
``` 

##¸ê®Æ·§Æ[¥î
§Ú¨Ï¥Î 'diamonds'¸ê®Æ¶°¡A¸ê°T¦p¤U:Šå?‚ä??:
```{r}
str(diamonds)
summary(diamonds)
```
##ª½¤è¹Ï
§Q¥Îggplot2 Ã¸µeªºª½¤è¹Ï
```{r echo = FALSE, message=FALSE}
library(plotly)
library(ggplot2)
static_ggx<-ggplot(diamonds  , aes(x),color=color) +
geom_histogram(binwidth = 0.5)+
facet_wrap(~ color)
ggplotly(static_ggx)
```

> Ralph Waldo Emerson : We have conquered the power, so we have the strength

![what message does R handle and created for you when it's correctly used looks like:](http://www.migflug.com/jetflights/wp-content/uploads/2015/03/reditt-com-mig-25-foxbat.jpg)
